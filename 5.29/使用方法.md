提取
# 必须指定配置文件路径（DXF、图层、输出文件名都在配置里）
# 导出线段（按配置里的 layers 和 output_filename）
python utils/entity_raw-0227.py --config utils/entity_export_config.json --mode line

# 导出文字（图层自动为全部）
python utils/entity_raw-0227.py --config utils/entity_export_config.json --mode text

# 也可在配置文件里改 "mode": "text"，然后只传 --config
python utils/entity_raw-0227.py --config utils/entity_export_config.json

# 示例：用 XJH 工程配置
python utils/entity_raw-0227.py --config ../XJH/config.json --mode line

阶段2：见 `step2A/README.md`、`step2B/README.md`。推荐命令：

```powershell
# Step 2A：分步运行
python step2A/run_init_graph.py --geo stage2/in/{图名}.json --stem {图名}
python step2A/square_bend.py --stem {图名}
python step2A/arc_bend_detect.py --stem {图名}
python step2A/arc_normalize.py --stem {图名}
python step2A/merge_normalized_geometry.py --stem {图名}
python step2A/build_normalized_graph.py --stem {图名}
python step2A/run_overview.py --stem {图名}

# Step 2B
python step2B/run_straight_wall.py --stem {图名}
python step2B/build_parallel_graph.py --stem {图名}

```

Step 2A 产物在 `step2A/raw/` 与 `step2A/output/`；Step 2B 产物在 `step2B/output/`。

step3A
python step3A/run_step3a.py --stem $stem

step3B
python step3B/build_residual_graph.py --stem {图名}

stage4
python stage4/classify_attached_regions.py --stem 2026.1-1part-巷道