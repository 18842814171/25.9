# 第5章 实验结果


### 5.1 巷道拓扑结构重建结果

对应 **3.2**。

内容：

* 整图中心线数量、主巷/辅巷/躲避硐数量等；
* 局部人工核验结果；
* 主巷/辅巷/躲避硐 P/R/F1；
* `structure_graph`、`attached_regions` 等结果图；
* 典型成功/失败案例。

---

### 5.2 工程对象语义重建结果

对应 **3.3**。

内容：

* 文字语义归类 P/R/F1；
* `final_cluster`；
* 设施候选数、分型数等；
* `facility_graph`；
* 典型测点、钻孔、设施识别结果。

---

### 5.3 空间关系与统一建模结果

对应 **3.4**。

内容：

* 工程对象挂接数量；
* **空间挂接正确率**；
* `structure_graph_with_texts`；
* **`structure_graph_with_facilities` 作为最终成果图**；
* 统一结构化模型案例。

这一节非常重要，因为它最终证明：

> 不是“识别出了几个对象”，而是已经把**对象 + 空间关系 + 语义**组织成统一结构。

---

### 5.4 跨矿井/跨图纸泛化实验（可选）

如果你的数据允许，可以保留。

但它不要承担第3章某个具体方法模块的验证职责，而是作为**整体方法泛化能力验证**。

---

## 还有一个地方我建议你一定改

你现在摘要里：

> “主巷道、辅巷道、躲避硐和巷道名称识别的加权综合 F1 值达到 0.97”

这个表述一定要和实验章节严格对应。

如果这个 **0.97 是10幅局部样本上的四类对象加权 F1**，就不要让读者误解成：

> 整个煤矿 CAD 图解析准确率 = 0.97

建议改成：

> 在10幅具有代表性的局部评价区域上，主巷道、辅巷道、躲避硐和巷道名称四类对象识别的加权综合 F1 值达到0.97。

或者更简洁：

> 在10幅局部评价样本上，主巷道、辅巷道、躲避硐和巷道名称四类对象识别的加权综合 F1 值达到0.97。

这样和你现在“**整图运行、局部评价**”的实验协议完全一致。

第5章 实验结果

5.1 巷道拓扑结构重建结果
5.2 工程对象语义重建结果
5.3 空间关系与统一建模结果
5.4 跨矿井/跨图纸泛化实验（可选）
```


---

# 第5章 实验结果（大纲）

## 5.1 几何—拓扑重建结果（对应 §3.2）

### 5.1.1 过程展示与过程统计（压缩原 5.1+5.2 叙述）

- 规范化：圆弧归一化、共线合并比例（过程量）。  
- 平行墙 → 主巷中心线；中位巷道宽度。  
- 残差修补：延伸/新增中心线数量。  
- 结构角色规模：辅巷、躲避硐、未分类（整图过程量）。

### 5.1.2 定量评价（核心；原 5.5 中结构部分挪入）

- 评价对象：主巷道 / 辅巷道 / 躲避硐（在 **补全后的 \(G_s\)** 上）。  
- 表：现表 5-5-2 前三行。  
- 误差分析：复杂交叉漏检、躲避硐漏检等。  
- **【待补】**：若补了中心线真值，加「中心线完整率 / 拓扑正确率」表；**未补则明确写占位，不写假准确率**。

### 5.1.3 建议配图（代码有现成 PNG）

| 优先级 | 图 | 代码产物 | 说明 |
|--------|----|----------|------|
| **必选** | 最终巷道结构图 \(G_s\) | `529-stage4/{stem}_structure_graph.png` | 3.2 最终输出 |
| **必选** | 结构角色着色 | `529-stage4/{stem}_attached_regions.png` | 主巷/辅巷/躲避硐 |
| **推荐** | 中心线修补前后对比 | `*_corridor_centerlines.png` + `*_centerline_fix.png` | 支撑“补全后评价” |
| 可选 | 平行关系图 | `*_parallel_graph.png` | 方法示意，不宜过多 |
| 不建议单独成节 | `*_step2a_overall.png`、`*_straight_wall.png` | 中间步骤，可放方法章或附录 |

整图细节难辨：用 **局部图** 出图；全图只报数字。

---

## 5.2 工程对象语义重建结果（对应 §3.3）

### 5.2.1 标注对象聚簇与文字归类（核心定量）

- 控制点/钻孔簇数量（过程量）。  
- **文字归类 P/R/F1**（现表 5-5-2 第四行）；**改正**原文「召回率 1.0000」→ 表中 **0.9483**。  

### 5.2.2 通风设施识别（降级为补充/案例）

- 候选数、分型成功数、未分型数、原因三句话（HATCH、线划—填充异构、尺度差）。  
- **不与主巷 F1 并列**；写「可行性验证 / 局限分析」。  
- 无可靠真值则 **不写设施类型 F1**。

### 5.2.3 建议配图

| 优先级 | 图 | 代码产物 | 说明 |
|--------|----|----------|------|
| **必选** | 最终标注簇 | `714-stage1/{stem}-final_cluster.png` | 控制点/钻孔实例 |
| **推荐** | 文字绑定链 | `{stem}-bind_chains.png` | 支撑聚簇机制 |
| 可选 | 候选簇 | `{stem}-candidate_cluster.png` | 冲突消解前后对比时用 |
| **推荐（案例）** | 设施实例图 | `714-stage2/{stem}-facility_graph.png` | 分型成功/未分型对比各 1 局部 |

---

## 5.3 空间关系与统一建模结果（对应 §3.4）

### 5.3.1 过程统计（现有，保留但降级）

- 成功挂接：控制点、巷道名称、设施数；钻孔独立节点说明。  
- 来源：`attach_summary` / `facility_attach_summary`。

### 5.3.2 关系正确性评价（**必补缺口**）

在若干局部图上人工核验挂接边：

| 指标 | 写法 |
|------|------|
| 挂接正确率 | \(Acc_{att}=N_{correct}/N_{checked}\) |
| 或分类型 | 控制点/名称/设施各自正确率 |
| 暂无数据 | 写 **[待补充：空间关系人工核验及挂接正确率]**，勿编 |

### 5.3.3 建议配图

| 优先级 | 图 | 代码产物 | 说明 |
|--------|----|----------|------|
| **必选** | 标注挂接后结构图 | `{stem}-structure_graph_with_texts.png` | 名称/控制点—中心线 |
| **必选** | 最终统一结构图 | `{stem}-structure_graph_with_facilities.png` | 论文最终输出示意 |
| 可选 | 错误挂接局部放大 | 手工在最终图上圈出 FP/漏挂 | 支撑关系评价（有真值后） |

---

## 5.4 跨矿井泛化实验（原 5.6，独立，不对应单一方法模块）

### 内容

- 同矿井表 + 跨矿井表（保留）。  
- **改正**「文字召回率均 1.0000」→ 表中 **0.5294 / 0.7458**。  
- 讨论：结构 F1 较稳、文字归类跨矿下降、辅巷跨矿漏检增加。  
- 摘要/结论中的 0.97 限定为：**主实验 10 幅局部四类对象加权 F1**，非“全算法综合准确率”。

### 建议配图

| 优先级 | 图 | 说明 |
|--------|----|------|
| **推荐** | 同矿 vs 跨矿各 1 张最终融合图 | 用各自 `structure_graph_with_facilities.png` |
| 可选 | F1 柱状对比图 | 非代码自动产物，可手绘/Excel |

---

# 评价指标：已有 / 应补 / 勿硬写

| 模块 | 指标 | 状态 | 行动 |
|------|------|------|------|
| 3.2 | 主/辅巷、躲避硐 P/R/F1 | ✅ 已有 | 挪到 5.1 作为结构重建主结果 |
| 3.2 | 中心线完整率、拓扑正确率 | ❌ 缺 | 在 **补全后 \(G_s\)** 上标真值后补；未补则占位 |
| 3.3 | 文字归类 P/R/F1 | ✅ 已有 | 修正召回率笔误 |
| 3.3 | 设施类型 P/R/F1 | ❌ 且分型差 | **不硬算**；案例+计数 |
| 3.4 | 挂接成功数 | ✅ 过程量 | 保留 |
| 3.4 | 挂接/关系正确率 | ❌ 缺 | **优先补**（抽样人工核验即可） |
| — | 加权 F1=0.9769 | ✅ | 写清适用范围 |
| — | 跨矿 F1 | ✅ | 修正文字召回笔误 |

---

# 章节结构对照（旧 → 新）

```text
旧 4.3 整图标定/局部验证/三类“评价指标”
  → 新 4.2 任务—指标表 + 指标定义；整图/局部缩为设置

旧 5.1 几何标准化 + 5.2 拓扑重建
  → 新 5.1 几何—拓扑重建（过程简写 + 结构角色 F1 + 待补中心线指标）

旧 5.4 多模态融合（混写 3.3+3.4）
  → 拆成 5.2 语义重建 + 5.3 统一建模

旧 5.5 抽样检测（四类混在一起）
  → 结构三类进 5.1；文字进 5.2；加权 F1 可放 5.2 末或 5.3 小结

旧 5.6 跨矿井
  → 新 5.4
```

---

# 配图总清单（按代码现状）

| 章节 | 加不加 | 用什么图 |
|------|--------|----------|
| 4.1 | 建议 1 张 | 数据/局部位置示意（手工） |
| 4.2 | 可不加 | 或方法—实验框图 |
| 5.1 | **加 2–3 张** | `structure_graph`、`attached_regions`；推荐修补前后 |
| 5.2 | **加 1–2 张** | `final_cluster`；设施用 `facility_graph` 作案例 |
| 5.3 | **加 1–2 张** | `structure_graph_with_texts`、`structure_graph_with_facilities` |
| 5.4 | **加 0–2 张** | 同矿/跨矿最终融合图对比 |

中间步骤图（规范化总览、直墙、平行图、残差图、候选簇）**优先放方法章示意或附录**，实验章避免“每步一张”冲淡任务—指标对应。

---

# 写作时务必改掉的两处事实笔误

1. 5.5：「巷道名称召回率 1.0000」→ **0.9483**（与表一致）。  
2. 5.6：「两组文字召回率均 1.0000」→ **0.5294 / 0.7458**。  
3. 局部验证：「直接调用整图识别规则」→ 改为「复用整图模板；规则在局部标定（工程加速；理想复用整图规则）」。

---

**一句话**：实验稿应按 **4.1 数据环境 → 4.2 任务与指标表 → 5.1§3.2 / 5.2§3.3 / 5.3§3.4 / 5.4 泛化** 重排；必补的是 **3.4 挂接正确率**，中心线几何 F1 可占位；图用代码已有的 **结构图 / 角色图 / 簇图 / 最终融合图**，中间步骤图少放。
5.1 巷道几何—拓扑重建结果

把这些程序输出作为一个完整链条：

wall_segment
   ↓
straight_wall
   ↓
parallel_graph
   ↓
centerline_graph
   ↓
residual_graph
   ↓
structure_graph

然后只选最有证明力的 2～3 张图：

1_straight_wall.png
1_corridor_centerlines.png
1_structure_graph.png

其余 JSON/PKL 作为数据输出统计，不需要逐个展示。

5.2 工程对象语义重建结果

把：

candidate_cluster
↓
final_cluster
↓
retrieved_elements_graph
↓
structure_graph_with_texts
↓
facility_graph

作为一个完整语义链。

选：

1-candidate_cluster.png
1-final_cluster.png
1-structure_graph_with_texts.png
1-facility_graph.png

但不一定四张全放，选最能说明问题的。

5.3 统一建模结果

直接重点展示：

1-structure_graph_with_facilities.png

这张是最终结果图。

再配：

1_pipeline_stats.json

里面的统计数据。

8. 甚至你现在的 pipeline_stats.json 很重要

这个文件我反而建议你重点利用。

因为它可以承担：

“程序执行结果的定量汇总”

而不是让你从十几个 JSON 中人工抄数字。

所以第5章可以形成：

图：

1-structure_graph_with_facilities.png

表：

pipeline_stats.json → 汇总统计表

指标：

根据人工真值 + 程序输出计算 P/R/F1 等

这样非常完整。

-------------

目标结构（推荐定稿）

```text
第5章 实验结果
  开篇：整图运行、局部展示/评价（一句协议即可）
  5.1 巷道几何—拓扑重建结果（§3.2）
  5.2 工程对象语义重建结果（§3.3）
  5.3 空间关系与统一建模结果（§3.4）← 现为空，重点补
  5.4 跨矿井/跨图纸泛化（原 5.5）

## 三、各节怎么写（对齐代码）

### 5.1 几何—拓扑（对应 `5.29` / `529-stage*`）

**过程量（压缩，半页内）**  
用整图或局部 `*_pipeline_stats.json`，不要逐步讲方法：

- 平行组 / 初始中心线数、`median_corridor_width`
- `centerline_fix`：`corridor_extensions`、`corridor_synthesized`
- `role_counts` / `semantic_counts`：corridor、auxiliary、niche、unclassified  

例：局部 `1_pipeline_stats.json` 可直接支撑叙述（中心线修补 3+1、角色规模等）。

**核心定量（原表 4—5—2 结构部分挪入，并按 4.3 口径重算）**

| 指标 | 口径（已与 4.3 一致） |
|------|----------------------|
| 中心线重建 P/R/F1 | **主+辅合并**，不拆主/辅分类 |
| 躲避硐 P/R/F1 | 单独一类 |

旧稿里「主巷 F1=0.9733、辅巷 0.9902」若保留，只能作附录或说明“曾按角色拆分”；**正文主表应改成中心线 + 躲避硐**，否则与 4.3 打架。

**配图（代码已有）**

| 必选 | 路径 |
|------|------|
| \(G_s\) | `529-stage4/{stem}_structure_graph.png`（局部可用 `lb-*`） |
| 角色着色 | `*_attached_regions.png` |
| 推荐 | `*_corridor_centerlines.png` + `*_centerline_fix.png` |

中间图（直墙、平行图等）不进实验章。

---

### 5.2 语义重建（对应 `7.14` / `714-stage1` + 设施 `714-stage2` 前半）

**核心**

- 过程量：`final_cluster.by_type`（控制点/钻孔数）
- 定量：**文字语义 P/R/F1**（原表第四行，召回写 **0.9483**）
- 设施：**降级为可行性案例**——候选数、分型成功/未分型、三点原因（HATCH、线划—填充异构、尺度差）；**不写设施类型 F1**

**从 5.2 删掉、挪到 5.3**

- 「成功关联控制点/巷道名称/设施」
- `structure_graph_with_texts` / `structure_graph_with_facilities` 作为“最终成果”的表述

**配图**

| 必选 | `714-stage1/{stem}-final_cluster.png` |
| 案例 | `714-stage2/{stem}-facility_graph.png`（成功/未分型各 1 局部即可） |

---

### 5.3 统一建模（对应挂接 + 最终图，现为空，必须写实）

**过程统计（可保留现 5.2 数字，但标明来源字段）**

- `attach_summary`：控制点 / 巷道名称挂接成功数；钻孔多独立
- `facility_attach_summary`：设施挂接成功数  

局部示例（stem=`1`）：控制点 13、巷道名称 24；设施挂接 37/42。

**核心定量（与 4.3 对齐，优先补）**

- **文字关联 P/R/F1**（锚点关联 + 巷道关联；孤立为合法关系）
- 若人工核验尚未做完：写 **`[待补充：文字关联人工核验及 P/R/F1]`**，勿编数
- 旧大纲的「挂接正确率」可并入本指标，不必另起一套

**配图（论文最终示意）**

| 必选 | `*-structure_graph_with_texts.png` |
| **必选（终稿图）** | `*-structure_graph_with_facilities.png` |

一句话收束：结果不是“检出若干对象”，而是 **对象 + 空间关系 + 语义** 已进统一结构图。

---

### 5.4 泛化（原 5.5）

- 保留同矿 / 跨矿两表，但类别列与 4.3 一致（中心线、躲避硐、文字语义；若有关联核验再加一列）
- 改正正文「文字召回均 1.0000」→ **0.5294 / 0.7458**
- 讨论：结构相对稳、文字跨矿降、辅巷/局部漏检增加（若改合并中心线，则谈中心线漏检而非“辅巷 F1”）
- 推荐配图：同矿 vs 跨矿各 1 张 `structure_graph_with_facilities.png`

---

## 四、旧 → 新内容搬家清单

```text
旧 5.1 规范化+拓扑过程叙述
  → 新 5.1.1 压缩为过程统计（半页）+ 配 Gs / 角色图

旧 5.2 聚簇+挂接+设施+最终融合图
  → 聚簇+文字F1+设施案例 → 新 5.2
  → 挂接数+最终图 → 新 5.3

旧 5.3（空）
  → 写入过程挂接 + 文字关联指标 + 两张融合图

旧 5.4 四类混评大表
  → 拆：中心线/躲避硐 → 5.1；文字语义 → 5.2；关联 → 5.3
  → 加权 F1 放 5.3 小结或 5.2 末，并写清「10 幅局部、哪些类」

旧 5.5 跨矿
  → 新 5.4，并改正召回笔误、对齐新类别
```

---

## 五、数字与摘要写法

1. **摘要/结论中的 0.97**：限定为「10 幅局部评价样本上，……加权综合 F1」，不要写成“全图 CAD 解析准确率”。  
2. 若正文主表改为「中心线 + 躲避硐 + 文字」，加权 F1 需按新类别重算，**不要继续引用旧四类加权 0.97 却配新表**。  
3. 整图过程数（如原文 5078 / 6022 / 辅巷 1874 等）与当前仓库里能扫到的 `pipeline_stats` 不完全对应；重构时**改从对应图纸的 `*_pipeline_stats.json` 重抄**，避免沿用无法复现的旧数。

---

## 六、写作优先级（可执行）

1. **先改结构**：填满 5.3，拆开 5.2，废除独立“抽样总表节”。  
2. **再对齐 4.3**：所有 F1 表改用中心线 / 躲避硐 / 文字语义 / 文字关联。  
3. **补文字关联人工核验**（5.3 唯一硬缺口）。  
4. **换图**：每节只挂上表中的代码 PNG；终稿图只用 `structure_graph_with_facilities`。  
5. **改两处召回笔误**；摘要同步加“局部样本”限定语。
