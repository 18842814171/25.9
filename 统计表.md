4.2.1 方法—实验对应表


| 方法模块 | 实验任务      | 输入（代码）                                      | 输出（代码）                            | 核心评价指标                               | 数据状态                            |
| ---- | --------- | ------------------------------------------- | --------------------------------- | ------------------------------------ | ------------------------------- |
| 3.2  | 巷道几何—拓扑重建 | `-巷道.json`                                  | `structure_graph`（step4B 后）       | 主/辅巷、躲避硐 P/R/F1；**【待补】中心线完整率/拓扑正确率** | 结构角色 F1 **已有**；中心线几何 F1 **缺**   |
| 3.3  | 工程对象语义重建  | `-文字.json`、`-设施.json`、$\Theta(\mathcal{B})$ | `final_cluster`、`facility_graph`  | 文字归类 P/R/F1；设施 **案例/计数**（暂不入核心 F1）   | 文字 F1 **已有**；设施分型 F1 **缺且不宜硬算** |
| 3.4  | 空间关系与统一建模 | $G_s$ + $O$                                 | `structure_graph_with_facilities` | **【待补】挂接/关系正确率**；过程量：挂接成功数           | 仅有挂接计数                          |
| —    | 跨矿井泛化     | 同矿/跨矿样本                                     | 同上对象                              | 各类 F1 及降幅                            | **已有**                          |


实验任务	所需指标	人工核验内容
巷道的几何—拓扑重建（3.2）	中心线重建性能（P/R/F1）	漏检中心线、位置错误中心线（不区分主、辅巷道）
	躲避硐识别性能（P/R/F1）	漏检躲避硐、几何错误躲避硐
工程对象的语义重建（3.3）	文字语义识别性能（P/R/F1）	文字分类错误数
结合空间关系的统一建模（3.4）	文字关联性能（P/R/F1）	文字关联错误数（关联锚点错误和关联巷道错误）

---

## 评价指标计算口径

| 方法模块 | 实验任务        | 核心评价指标           | 评价内容解释                                                                                                                                                       | 基数                                                                                                                                 | 差错记录规则                                                                                                                                                                                                                                                                        | 换算                                                                                                                                   | 人工核验内容                  |
| ---- | ----------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------- |
| 3.2  | 巷道几何—拓扑重建   | 中心线重建性能（P/R/F1）  | 巷道中心线是否完整重建、位置是否正确。主巷与辅巷图面边界模糊、代码亦难严格区分，故合并为同一类“中心线”，不单独考核主/辅分类。                                                                                             | $N_{\mathrm{GT}}^{\mathrm{cl}}$：局部样本 `structure_graph` 中主巷与辅巷中心线数量之和（`role_counts.corridor + role_counts.auxiliary`）。躲避硐、未分类线段不计入。 | 漏检中心线 → 1 FN；位置错误中心线 → 1 FP + 1 FN。                                                                                                                                                                                                                                           | $N_{\mathrm{FN}}=N_{\mathrm{miss}}+N_{\mathrm{pos}}$，$N_{\mathrm{FP}}=N_{\mathrm{pos}}$，$N_{\mathrm{TP}}=N_{\mathrm{GT}}-N_{\mathrm{FN}}$（miss=漏检，pos=位置错误）。 | 漏检中心线、位置错误中心线（不区分主、辅巷道） |
| 3.2  | 巷道几何—拓扑重建   | 躲避硐识别性能（P/R/F1）  | 躲避硐是否检出，以及几何形态是否正确。                                                                                                                                          | $N_{\mathrm{GT}}^{\mathrm{ni}}$：局部样本 `role_counts.niche`。                                                                          | 漏检躲避硐 → 1 FN； 几何错误躲避硐 → 1 FP + 1 FN。                                                                                                                                                                                                                                          | 同上                                                                                                                                   | 漏检躲避硐、几何错误躲避硐           |
| 3.3  | 工程对象语义重建    | 文字语义识别性能（P/R/F1） | 文字图元的语义类别是否正确。语义分类固定为巷道名称、钻孔文字、控制点文字3 类；结果中 不出现“未分类”文字，故不设未分类相关差错项。                                                                                          | $N_{\mathrm{GT}}^{\mathrm{tx}}$：局部样本中参与语义评价的文字（或文字簇）数量，须与人工核验范围一致。                                                                 | 文字分类错误（已检出但类别错）→ 记 1 FP；因对象已被检出，**不另计 FN**； 若存在应识别文字完全未出现，另计漏检文字 → 1 FN（与分类错误分开统计）。                                                                                                                                                                                           | 在已约定 GT 与 FN/FP 后，$N_{\mathrm{TP}}=N_{\mathrm{GT}}-N_{\mathrm{FN}}$，再算 P/R/F1。                                                       | 文字分类错误数                 |
| 3.4  | 结合空间关系的统一建模 | 文字关联性能（P/R/F1）   | 文字关联关系是否正确，包括 **与锚点关联** 与 **与巷道关联** 两类。同一文字若两类均错，按 **一条关联单元计 1 次错误**（去重）。并非所有文字都必须关联到巷道。“孤立”（不挂接巷道、或不绑定锚点）视为一种 **合法关系**。因此 **不单列“漏关联”** 指标，也不把“未挂接巷道”一律当作缺陷。 | $N_{\mathrm{GT}}^{\mathrm{att}}$：局部样本中参与关联评价的文字关联单元数（与人工核验范围一致；含判定为“孤立”的单元）。                                                       | （统一记入“文字关联错误数”，**不设漏关联**）：关联锚点错误（应绑 A 而绑到 B，或应孤立却误绑锚点，或应绑锚点却被判孤立等）→ 按单元计错误；关联巷道错误（应挂巷道 $c$ 而挂到 $c'$，或应孤立却误挂巷道，或 **应存在巷道/锚点关联却被判为孤立** 等）→ 按单元计错误；上述错误关联 → **1 FP + 1 FN**（将错误的关系判定视为一次错误检出）。== 说明 ==：所谓“应该存在关联却未关联”，在本口径下 **不算单独的漏检项**，而计入 **关联错误**——因为算法给出的“孤立”关系相对于应有关联是错误的。 | $N_{\mathrm{FN}}=N_{\mathrm{err}}$，$N_{\mathrm{FP}}=N_{\mathrm{err}}$，$N_{\mathrm{TP}}=N_{\mathrm{GT}}-N_{\mathrm{FN}}$（err=关联错）。                 | 文字关联错误数（关联锚点错误和关联巷道错误）  |

---

### 评价指标计算口径（条目版）

1. 方法模块：3.2；实验任务：巷道几何—拓扑重建；核心评价指标：中心线重建性能（P/R/F1）；评价内容解释：巷道中心线是否完整重建、位置是否正确。主巷与辅巷图面边界模糊、代码亦难严格区分，故合并为同一类“中心线”，不单独考核主/辅分类；基数：$N_{\mathrm{GT}}^{\mathrm{cl}}$：局部样本 `structure_graph` 中主巷与辅巷中心线数量之和（`role_counts.corridor + role_counts.auxiliary`）。躲避硐、未分类线段不计入；差错记录规则：漏检中心线 → 1 FN；位置错误中心线 → 1 FP + 1 FN；换算：$N_{\mathrm{FN}}=N_{\mathrm{miss}}+N_{\mathrm{pos}}$，$N_{\mathrm{FP}}=N_{\mathrm{pos}}$，$N_{\mathrm{TP}}=N_{\mathrm{GT}}-N_{\mathrm{FN}}$（miss=漏检，pos=位置错误）；人工核验内容：漏检中心线、位置错误中心线（不区分主、辅巷道）。

2. 方法模块：3.2；实验任务：巷道几何—拓扑重建；核心评价指标：躲避硐识别性能（P/R/F1）；评价内容解释：躲避硐是否检出，以及几何形态是否正确；基数：$N_{\mathrm{GT}}^{\mathrm{ni}}$：局部样本 `role_counts.niche`；差错记录规则：漏检躲避硐 → 1 FN；几何错误躲避硐 → 1 FP + 1 FN；换算：同上；人工核验内容：漏检躲避硐、几何错误躲避硐。

3. 方法模块：3.3；实验任务：工程对象语义重建；核心评价指标：文字语义识别性能（P/R/F1）；评价内容解释：文字图元的语义类别是否正确。语义分类固定为巷道名称、钻孔文字、控制点文字 3 类；结果中不出现“未分类”文字，故不设未分类相关差错项；基数：$N_{\mathrm{GT}}^{\mathrm{tx}}$：局部样本中参与语义评价的文字（或文字簇）数量，须与人工核验范围一致；差错记录规则：文字分类错误（已检出但类别错）→ 记 1 FP；因对象已被检出，不另计 FN；若存在应识别文字完全未出现，另计漏检文字 → 1 FN（与分类错误分开统计）；换算：在已约定 GT 与 FN/FP 后，$N_{\mathrm{TP}}=N_{\mathrm{GT}}-N_{\mathrm{FN}}$，再算 P/R/F1；人工核验内容：文字分类错误数。

4. 方法模块：3.4；实验任务：结合空间关系的统一建模；核心评价指标：文字关联性能（P/R/F1）；评价内容解释：文字关联关系是否正确，包括与锚点关联与与巷道关联两类。同一文字若两类均错，按一条关联单元计 1 次错误（去重）。并非所有文字都必须关联到巷道。“孤立”（不挂接巷道、或不绑定锚点）视为一种合法关系。因此不单列“漏关联”指标，也不把“未挂接巷道”一律当作缺陷；基数：$N_{\mathrm{GT}}^{\mathrm{att}}$：局部样本中参与关联评价的文字关联单元数（与人工核验范围一致；含判定为“孤立”的单元）；差错记录规则：统一记入“文字关联错误数”，不设漏关联。关联锚点错误（应绑 A 而绑到 B，或应孤立却误绑锚点，或应绑锚点却被判孤立等）→ 按单元计错误；关联巷道错误（应挂巷道 $c$ 而挂到 $c'$，或应孤立却误挂巷道，或应存在巷道/锚点关联却被判为孤立等）→ 按单元计错误；上述错误关联 → 1 FP + 1 FN（将错误的关系判定视为一次错误检出）。说明：所谓“应该存在关联却未关联”，在本口径下不算单独的漏检项，而计入关联错误——因为算法给出的“孤立”关系相对于应有关联是错误的；换算：$N_{\mathrm{FN}}=N_{\mathrm{err}}$，$N_{\mathrm{FP}}=N_{\mathrm{err}}$，$N_{\mathrm{TP}}=N_{\mathrm{GT}}-N_{\mathrm{FN}}$（err=关联错）；人工核验内容：文字关联错误数（关联锚点错误和关联巷道错误）。
